# ==============================================================================
# GeoViable — Python Dependencies
# ==============================================================================
# Pinned versions for reproducibility. Built for Python 3.11 + linux/arm64.
# ==============================================================================

# ── Core Framework ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ── Database & GIS ──
sqlalchemy==2.0.36
geoalchemy2==0.17.1
psycopg2-binary==2.9.10
shapely==2.0.6
pyproj==3.7.0

# ── GeoPandas (for update_layers.py shapefile processing) ──
geopandas==1.0.1
# GDAL is provided by the system package (gdal-bin) in the Docker image.
# The Python binding version must match the installed GDAL library.
gdal==3.6.2

# ── PDF Generation ──
weasyprint==63.1
jinja2==3.1.5

# ── Static Map Generation ──
contextily==1.6.2
matplotlib==3.9.4
pillow==11.1.0

# ── HTTP & Web Scraping ──
requests==2.32.3
beautifulsoup4==4.12.3

# ── Testing ──
pytest==8.3.4
httpx==0.28.1          # async test client for FastAPI
pytest-asyncio==0.25.0
